Closes #125。

## 改了什么

按 Issue #125 修订版方案（具名段下沉、key 契约归事实归属包）完成观测桥正式化，分三层：

**1. 信封与契约层（observability + llm/loop/flow）**

- `Record` 新增 `Attrs` 开放段：值域经泛型 `Set[T AttrValue]` / `Get[T AttrValue]` 锁死在标量（`~string|~int64|~float64|~bool`，含命中约束的命名类型）。Go 的 union 接口不能作 map 值类型，故 `Attrs` 为 struct 内核 + 包级泛型写读入口——`[]byte`/struct/slice/任意对象在类型上无法进入，`map[string]any` 逃生舱不存在（隐私声明按 Issue 修订版表述：载荷类型上进不来 + 标量 kv 开放 + key 自述意图 + Sink 侧 redact 钩子）。
- `SlogSink` 的 Attrs 段按 key 字典序输出且恒位于具名字段之后；`Attrs.MarshalJSON` 同序（出口确定性）。
- key 契约常量：`llm.AttrModel/TokensIn/TokensOut/TokensCached`（新建 `llm/obs.go`）、`loop.AttrTool/AttrSteps`、`flow.AttrNode`——均为 `<组件>.<字段>` 点分纯 string 常量，零依赖。
- `llm.Response` 新增 `Model` 字段（llm 层自行适配的第一个实例）：anthropic 非流式 + 流式 `message_start` 累积、openai responses 三处、openai completions 非流式 + 流式首 chunk 捕获，共 6 处构造点填充；流式协议首事件前不暴露 model 允许零值。

**2. 桥正式化（新增 observability/bridge）**

- `Attach(scope, Config)`：挂 llm/loop 运行期监听 + 注册 `CollectorKey` 服务；scope 销毁自动摘除。
- 折叠映射：`before_generate` 只计时（waterfall 透传）、`after_response` → `llm.generate_finished`（model/token 进 Attrs）、`after_tool_call` → `loop.tool_finished`（completed|failed|rejected）、`turn_end` → `loop.turn_finished`（steps；token 不重复记）；**`before_tool_call` 刻意不订阅**（HITL 中立，测试锚钉死审批监听恰被调到一次）。
- `FlowObserver()`：flow 节点 wait/run 分段计时，nodeID 走 `flow.AttrNode`，不再占用 `FiberName`。
- `CollectorKey`（`pulse.observability.collector`）服务化直写：业务插件 `kernel.Get` 后 `Write/WriteAttrs`，HostID/TraceID 自动携带——Web 框架地基的入口形态。
- `New(cfg)`：无 scope 纯出口构造（纯图运行 / 直写用）。

**3. 消费者改造（demoapp + 04-flow）**

- 删除 demoapp 手写 `bridge.go`（60 行 + 三个 hack 全部消除：token slog 旁路 → Attrs 契约；FiberName 挪用 nodeID → `flow.AttrNode`；裸字符串事件名 → 导出常量）；`FlowPeak` 迁至 `flowpeak.go` 并提供 `Observer()`，与官方桥经 `flow.MultiObserver` 组合。
- `Host.NewBridge` 返回官方 `*bridge.Bridge`；04-flow 的 dag/rag/main 与测试全部改用官方包。

## 新增

- `observability/bridge/`（bridge.go / observer.go / service.go + bridge_test.go + 双语 README）
- `llm/obs.go`（key 契约）
- `observability/attrs_test.go`、`observability/sink_test.go`

## 影响范围

- `observability`：Record/Attrs 公开面扩展（新字段 + 新 API），既有 Sink 接口与 Bootstrap 行为不变。
- `llm`：`Response.Model` 为新增字段，向后兼容；两 adapter 的响应构造点填充。
- `loop`/`kernel/flow`：仅新增常量，零行为变化。
- demoapp/04-flow：示例层改消费者，对外行为不变（记录形态变化：nodeID 从 FiberName 移到 Attrs、token 从 slog 旁路进 Attrs）。
- 设计文档 §2/§3.1/§7 就地修订 + 新增 §9；决策记录补 D7–D10。

## 测试

- `go test -race -skip TestLive` 权威清单 27 包全绿（含新增 observability/bridge）；`./eval/` -race 绿；plan9/amd64 + js/wasm 交叉编译绿；gofmt 新文件干净（点名 5 个既有文件为 autocrlf 全文件±噪声，不修）。
- 新增测试锚：Attrs 四类标量往返/命名标量/类型不符/JSON 排序；SlogSink attrs 排序与具名字段前后关系；bridge 全链路（Registry 装配 scripted agent，逐条断言信封与 attrs）、双 scope TraceID 隔离 + Dispose 摘除、HITL 中立、Collector 直写、FlowObserver 分段（Duration>0）/skip/双节点 nodeID 隔离。

## review 关注点

1. **Attrs 内核形态**：Go 类型系统限制下（union 接口不能作变量类型）选了「struct 内核 + 包级泛型 Set/Get」，代价是放弃 map 字面量构造；`Get` 的命名类型转换走 reflect（kind 核对后转换，类型不符返回 false 不 panic）。是否接受该取舍请审。
2. **turn_end 不记 token**：token 以 after_response 单次口径为准、桥不做累计，避免与 AttrTokensIn/Out「单次口径」注释混淆。如需回合累计口径应另立 key，本期不做。
3. `TokenUsage` 字段是 `int`，桥折叠处显式 `int64()` 收窄（约束保持 `~int64` 最小值域）。
4. 设计文档 §7 原「Collector 概念本身」一条按 #125 拍板推翻落地（D10），已就地修订并注明被否的仍是 D2 字符串总线。
