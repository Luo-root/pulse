## 审核结论：#127 基座部分实现干净，1 处 must-fix（gofmt 定性）后可继续按序推进

### 已核实

- **collector.go 实现质量好**：`ObserveConfig` godoc 把生命周期语义钉死（同一请求复用即 D3 关联、跨请求必须新建）；`ErrNilScope`/`ErrNilSink` 哨兵与「重复 Attach 按 Provide 覆盖语义」均落（#127 审核交叉项 3 全部吸收）；`Collector` 封装收敛为私有字段 + 仅 `Write/WriteAttrs` 暴露，比 #126 的 `Collector=*Bridge` 形态更好——业务插件拿不到 FlowObserver 等内部面。
- **SourceAdapter 更名**：15 处引用全改（record.go 常量 + 注释注明历史名、bridge 过渡期、02-react、demoapp、04-flow、测试），字面值 `"bridge"` 不变兼容既有日志解析——#127 审核交叉项落实 ✓。
- **上次 #126 审核指出的 record.go 2 行 `//（` 脏已修** ✓；新增文件（collector.go / collector_test.go）blob 层面 gofmt 干净 ✓。
- **验证**：`go build ./...` 全仓 OK（bridge 过渡期编译）；`go test -race -count=1` 下 observability / bridge / llm / llm.openai / llm.anthropic / loop / 04-flow / 07-production / demoapp 全绿。
- 滚动式 PR 结构清晰，#128–130 待续清单与我 #127 审核意见对齐（D5 修订、隔离锚都在收尾/子票范围）。

### must-fix：gofmt 定性错误第三次出现，且与 #127 验收 5 冲突

PR body 再次写「既有文件点名为 autocrlf 全文件±噪声，不修」。**#126 审核已经用 git blob 层面验证纠正过这个定性**：这些文件 blob 全部 LF 无 CRLF，真实差异是 gofmt 1.19+ 中文注释规范化（`//（` → `// （`）、struct 注释列对齐、以及 flowpeak_test.go 的 BOM——与 autocrlf 无关。

本次实测（blob 层面）6 个文件真实 dirty，全部 main 既有（demoapp_test.go 是本次改到才暴露）：

```
4  changes  examples/04-flow/main.go
2  changes  examples/04-flow/main_test.go
5  changes  examples/internal/demoapp/demoapp_test.go
2  changes  examples/internal/demoapp/flowpeak_test.go  （BOM=True）
5  changes  examples/internal/demoapp/host.go
14 changes  loop/events.go
```

两个动作：
1. PR body 定性改为「gofmt 1.19 注释规范化既有脏 + BOM」，别再写 autocrlf；
2. **#127 验收 5 明文「gofmt 干净」**——这 6 个文件全部在票面迁移路径上（demoapp/04-flow 装配改写、loop 在 #129 范围），建议收尾 commit 顺手修掉（合计 ~30 行注释级差异，工作量分钟级），验收 5 才能严格核销。

### 建议（非阻塞，随 #128–130 落地一起看）

`Collector.write(event, status, d, err, set)` 的 `d`/`err` 参数是死代码：两个公开入口恒传 0/nil，Collector 直写面没有任何入口能设置 Duration/Err（Record.Err 信封字段对业务插件不可达）。若按「直写状态型事实」定位则收敛签名为 `write(event, status, set)`；若认为业务直写需要失败记录，补一个带 `error` 的入口。二选一，别留无用参数。

### 结论

#127 基座部分可以按这个形态继续；改掉 gofmt 定性 + 收尾时修齐 6 文件后，本票验收 5 可核销。按序推进 #128 时记得把双 scope 隔离 + Dispose 摘除锚带上（#127 审核意见 2）。
