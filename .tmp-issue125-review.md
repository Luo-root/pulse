## 设计审核：方向成立，3 处票面修订 + 1 处契约补齐后可进实现

票面引用的工程债与仓库事实已逐行核对，全部准确：

- `examples/internal/demoapp/bridge.go:63-68` token 走 slog 旁路 ✓（另 `bridge.go:93-100` turn_end 摘要同样只走 slog.Info，属同类旁路，本票一并收编 ✓）；
- `bridge.go:154` 注释自认「FiberName 借官方信封的实例诊断名槽位填 nodeID」——语义漂移属实 ✓；
- 手写桥约 70 行（install 订阅 + 折叠），每宿主重复 ✓；
- `observability/record.go:41-42`「隐私边界由编译器保证：没有 Attributes map 或任何任意 kv 注入口」与 `record.go:46-47`「不要扩本结构」原声明 ✓——本票的改写对象真实存在；
- 事件键全部存在且口径正确：`llm.EventBeforeGenerate`（Waterfall，registry.go:24）、`EventAfterResponse`（registry.go:25）、`loop.EventBeforeToolCall`（Waterfall，loop.go:222）、`EventAfterToolCall`（**自带 Duration**）、`EventTurnEnd`（带 Usage/StoppedBy/Steps）；
- SlogSink（sink.go:9）、flow.Observer、scope Dispose 自动摘除监听（kernel 销毁链「清空事件总线」）、D3 两层标识——机制前提全部成立 ✓。

双层观测模型（具名信封 + attrs 扩展面）、适配责任下沉、bridge 只做机制——这三个核心判断我都认可，OTel 验证过的形状没有问题。以下是修订项。

### 1. 治理缺口：设计文档 L150「Record map 逃生舱」决策记录未显式重开

observability-v1-design.md L150 的「不做」清单明确列有 **「Record map 逃生舱」**，且文档自己的规则是「每一条『不做』都对应一轮评审的具体反对意见；重开时需先推翻对应决策记录」。本票实质就是重开该条款，但票面只改写了 record.go 的防线表述，没有点名 L150。

请补：本票即对 L150「Record map 逃生舱」的重开决议，论证口径 = D2 否决的是**无类型 any** 逃生舱（字符串总线 + `map[string]any`），Attrs 值域被泛型标量约束锁死（`~string|~int64|~float64|~bool`），payload 结构（Message 切片/附件/思维链）在类型上进不来——这不是 D2 意义上的逃生舱。同时把 L150 与 L47（「token 数走 SlogSink 附加键」段）列入配套修订清单。

### 2. Waterfall 理由自相矛盾：before_generate 订阅 vs before_tool_call 不订阅

折叠表订阅 `llm.before_generate` 并标注「Waterfall，只观察」——桥以观察者身份进入 Waterfall 链（现状 `bridge.go:42` 正是这么做的，恒 `next(req)`）；而「不做」段对 `loop.before_tool_call` 的排除理由却是「它是 Waterfall HITL 挂载点，桥以观察者身份进入会污染用户审批链」。

同一个理由（观察者进入 Waterfall）在两个事件上给出相反决策，读者会问「为什么 before_generate 不污染」。真正自洽的差异是：

- **before_generate 必须订阅**：`llm.after_response` 不携带耗时（Response 无 Duration 字段），Waterfall 回调是唯一可行的计时起点，无替代；
- **before_tool_call 无需订阅**：`AfterToolCall.Duration` 事件自带（`bridge.go:86` 现状就在用），订阅无观测增益；
- 恒 `next` 且不改参数的观察者不改变 Waterfall 语义——「污染」论对两者要么都成立要么都不成立。

建议把不订阅的理由改写为「无观测增益 + 少一份与 HITL 链的顺序耦合」，去掉「污染」表述。行为决策本身（订一个、不订一个）是对的，只是理由要换。

### 3. Attrs 引用语义缺契约条款：Record 第一个引用类型字段

现有 Record 字段全为值类型（string/int/error/time），Attrs 是第一个引用类型（map）。`MultiSink` 扇出会把**同一个 map** 递给多个 Sink；Sink 契约目前只有「实现必须并发安全且不得长时间阻塞调用方」。缺的条款：

- 产出方（桥/业务插件）每次构造独立 Attrs，`Write` 返回后不再修改；
- Sink 实现不得修改 Record 及其 Attrs；异步导出器（如未来 OTLP 队列化）需自行拷贝后再持有。

不补的话，未来某个异步 exporter 就是埋好的 race。建议验收标准加一条：MultiSink 多 Sink 并发扇出同一带 Attrs 的 Record，`-race` 下无竞争。

### 建议项（非阻塞）

- **分层声明同步清单补全**：桥从 demoapp 升级为 `observability/bridge` 后，`record.go:4-8` 包注释（「本包只 import kernel……桥……不经过本包」）、`doc.go` 的「token 走 SlogSink 附加键」表述、README 的分层图都以「桥在 demoapp」为前提，需一并同步为「observability 本体不 import llm/loop/flow；bridge 子包是装配层适配器（依赖方向不变，无环）」。票面配套只列了设计文档 + observability/README。
- **int→int64 转换摩擦**：`TokenUsage.InputTokens` 是 `int`，AttrValue 约束 `~int64`——折叠处须显式 `int64(...)`。维持 ~int64 不加 ~int 是对的（32 位平台值域混乱），票面可补一句实现注意，避免想当然赋值编译不过。
- **turn_end 的 token 口径**：`llm.tokens_in/out` 在 after_response 是单次、在 turn_end 是回合累计——同 key 不同口径，依赖事件类型区分。key 常量注释里写清口径即可（避免下游把累计值当单次值做图）。
- **业务 key 冲突**：Collector.Write 开放后 `<组件>.<字段>` 对业务插件无注册机制可防冲突（OTel 同样靠约定）。建议文档约定业务插件用包路径做前缀。

### 结论

三个工程债引用全部核实准确，验收标准 6 条可测，「不做」清单边界清晰（不改 kernel 事件系统、不做 OTel exporter、不动 demoapp 定位、本体 import 面不变）。修订第 1–3 项（L150 决策重开 + Waterfall 理由改写 + Attrs 并发契约）后即可进实现；建议项随文档同步自然落。
