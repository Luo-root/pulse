## 审核结论：核心实现质量高，2 处 must-fix 补齐后可合

#125 审核提出的三项修订全部落实，且处理质量超出要求：

- **L150「Record map 逃生舱」精确化决议**（§7）：「本条不做指 D2 意义上的无类型 any 逃生舱……本票即对该条款的重开与收窄记录」+ 把原「Collector 概念本身」一条的推翻也显式化（D10：被否的是 `Collector.Emit` 字符串总线，不是宿主直写）——治理记录完整，后续无悬案；
- **Waterfall 理由自洽化**（§9.2 + bridge.go 包注释）：before_generate「必须订阅——唯一可行计时起点，恒 next 不改语义」vs before_tool_call「刻意不订阅——AfterToolCall 自带 Duration/Err 无增益」——差异归因于「有无替代计时手段」而非 Waterfall 本身，自洽 ✓；
- **Attrs 引用语义契约**：三条契约写入 Sink godoc + `TestMultiSinkConcurrentAttrsFanout` race 锚 ✓；
- 分层声明同步（record.go/doc.go 包注释、§2 分层图、双语 README）、int→int64 显式收窄（bridge.go:136-138）、业务 key 前缀约定（§9.2）均落地。

### 核心取舍（PR 请审点 1）：Attrs 内核形态——我认可

Go 的 union 约束接口不能作 map 值类型，`Attrs map[string]AttrValue` 在编译期就是死路。struct 内核（kind tag 定长联合）+ 包级泛型 `Set[T]/Get[T]` 是该约束下的正解，且比票面的 map 形态**更严**：`AttrValue` 只能出现在泛型约束位置，「绕过约束直接构造值」的口子被类型系统堵死。`Get` 先直接断言、再反射 kind 核对后才转换（类型不符返回 false 不 panic）、命名标量（`~int64` 别名）支持、`MarshalJSON` 按 key 排序——实现干净。代价（放弃 map 字面量构造、`WriteAttrs` 回调式写入）可接受。

### 独立验证记录

- `-race` 权威清单 27 包全绿（observability 2.4s / bridge 2.1s 实跑）；`./eval/` -race 绿；plan9/amd64 + js/wasm 交叉编译绿；
- **import 边界全绿**：observability 本体零 llm/loop/flow import；llm/loop/kernel/flow 零反向 import observability；bridge 恰好 import kernel+llm+loop+flow+observability 五包，与 §9 一致；
- llm `Response.Model` 六处构造点逐一核对（anthropic 非流式 + 流式 message_start 累积；openai completions 非流式 + 首 chunk 捕获；responses 非流式 + completed/incomplete 两分支）；
- 折叠映射逐条对 §9.2 定案（透传、三态 Status、turn_end 不记 token 的口径注释）；
- 测试锚与 #125 验收逐条对上（全链路 / 双 TraceID 隔离 + Dispose / HITL 中立 / Collector 直写 / 分段计时 + skip / nodeID 隔离）；抽查 `TestBridgeIsHITLNeutral`：审批监听计数锚 + 拒绝工具未执行 + rejected 如实记录，走真实 Registry 装配 loop.Agent，非假测试。

### must-fix 1：redact 示例与测试锚缺失（#125 验收 5 后半句）

验收 5 原文：「SlogSink：Attrs 按 key 排序输出；**redact 示例（宿主 Sink 拒绝敏感 key）有测试锚定**」。前半已锚定（TestSlogSinkAttrsSorted），后半**零落地**——全仓库仅 record.go:50 godoc 提及 redact，无示例实现、无测试。PR 声称 Closes #125，不能静默丢验收项。

补法很轻（二选一）：
- demoapp（宿主侧示例的正确位置）加一个 `RedactSink` 包装示例——核心即 `Write` 里检查 `r.Attrs` 的 key 前缀/黑名单，命中则拒绝或抹除该 key——配一个测试（带敏感 key 的记录进入后断言被拒/被抹）；
- 或在 `observability` 包测试里以文档化示例形式锚定。

若认为该项应属宿主自由而非验收项，请在 PR 或 issue 里显式声明并修订验收文字——不能不提。

### must-fix 2：gofmt 定性与事实不符，且 PR 新引入 2 行脏

PR body：「gofmt 新文件干净（点名 5 个既有文件为 autocrlf 全文件±噪声，不修）」。实测（对 **git blob** 层面验证，排除工作区 autocrlf 干扰）：**6 个文件真实 dirty，没有一个是 autocrlf 噪声**（blob 全部 LF、无 CRLF）。真实差异是 gofmt 1.19+ 的中文注释规范化（`//（` → `// （`）、struct 注释列对齐、以及 `flowpeak_test.go` 的 **BOM**（blob 层面带 `\xef\xbb\xbf`）：

- `observability/record.go`：**PR 新增注释引入**（`//（如 llm.model…` 2 行）——应修，自己新增的行要 gofmt-clean；
- `examples/internal/demoapp/host.go`：PR 新增段带入部分差异——顺手修；
- `examples/04-flow/main.go`、`main_test.go`、`loop/events.go`、`flowpeak_test.go`（含 BOM）：main 既有脏——可按 diff 最小不修，但请在 PR body 把定性从「autocrlf 噪声」改为「gofmt 1.19 注释规范化既有脏 + BOM」，避免后续维护者误判格式状态。

### 建议（非阻塞）

- `Bridge.genStart` 是单值：同一 scope 若并发多次 `Generate`（多 agent 共享请求 scope），计时起点互相覆盖、`generate_finished` 的 Duration 失真。v1 单请求单 agent 串行模型下无影响，建议 godoc 注明「同 scope 并发生成时计时不定义」。

### 结论

设计文档、契约、实现、测试、边界五个层面核对完毕，质量扎实。补齐 must-fix 1/2 后即可合并。
