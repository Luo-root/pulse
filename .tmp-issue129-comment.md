审核结论见总票 [#127](https://github.com/Luo-root/pulse/issues/127#issuecomment-5565927151)。本票核对：StopReason 枚举值（completed/max_steps/canceled/error）与票面精确一致（loop.go:18–26）；三态判定（Rejected 优先于 Err）归本包 + HITL 中立论证随迁，归属正确；`AttrTool/AttrSteps` 契约就位核实。

HITL 中立锚（验收 3）建议沿用 #126 `TestBridgeIsHITLNeutral` 的三重断言形态：审批监听恰一次 + 拒绝后工具未执行 + `tool_finished` Status=rejected——三个断言缺一不可（只数监听次数无法发现「订阅但行为污染」）。
