审核结论见总票 [#127](https://github.com/Luo-root/pulse/issues/127#issuecomment-5565927151)。本票核对：环论证正确（kernel/flow → observability → kernel，不同包路径无环）；折叠映射与 #126 已实测的 FlowObserver 分段逻辑一致（wait/run 两条、skip 单条等待、nodeID 走 AttrNode 不占 FiberName）；验收 1–5 可测，组合锚（MultiObserver 与宿主 Observer 互不干扰）保留了好习惯。

两点提示：① `EventNodeWaitFinished/RunFinished` 常量迁入后，demoapp `FlowPeak` 等既有消费者的事件名引用同步改 import（迁移清单已含）；② 内部按 nodeID 记账的 map 在节点异常路径（Finished 未达）会残留条目——与 #126 实现同构，Observer 生命周期与图运行同周期时无害，godoc 注明「单次图运行一个适配器实例」即可。
