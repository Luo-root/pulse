## 总设计审核：双基座论证成立，两处必须补齐后子票可开工

### 双基座模型核实（核心论证）

三个支撑事实全部在仓库核实成立：

1. **「插件形态听不到」论证正确**：插件 Apply 拿的是私有子作用域，其上注册的监听只在该子 scope 生效；而 llm/loop 的运行期派发走 EmitLocal/WaterfallLocal（registry.go 多处），只本 scope 可见、不上浮——监听若挂在插件 Apply 的私有子 scope，确实听不到请求 scope 上的 Local 派发。导出 `Observe(scope, cfg)` 挂宿主传入的 reqScope（与 Bootstrap 同构）是唯一可行装配面。
2. **环论证正确**：kernel/flow → observability → kernel 无环（不同包路径，Go 允许）；方案 A 收紧为「本体只 import kernel 且无任何例外」后全仓无逆箭头。
3. **对称性成立**：llm 已依赖 kernel 发事件，再依赖 observability 折记录，箭头方向一致。

#126 的审查成果（Record/Attrs 内核、Sink 契约、key 契约、Response.Model、SlogSink 排序）原样保留进基座，符合「#126 成果保留、形态重定」的预期。

### 必须补齐 1：D5 决策修订不在迁移清单里

observability-v1-design.md 的 D5 原文否决「**在 before_generate 等 Waterfall 上挂计量**」（原型 bug：观测进 around 链短路真实流程）。#128 落地的 `llm.Observe` 恰恰在 `before_generate`（Waterfall）上挂计时——**不改 D5，设计文档与实现直接冲突**。

#126 曾用「本体侧禁令 + bridge 例外」调和；bridge 删除后例外主体变为 `llm.Observe`，D5 决策行与 §7 对应条款必须同步重写（精神保留：旁路无副作用——恒 next 且不改参；字面修正：计量起点例外归 llm.Observe）。迁移清单「设计文档 §2/§7/§9 重写（D9/D10 重述 + 依赖纪律增补）」需补 **D5**。

### 必须补齐 2：双 scope 隔离 + 监听 Dispose 摘除的回归锚在子票验收中丢失

#126 的 `TestTraceIDIsolationAndDispose` 钉死两个行为：Local 派发按 scope 严格隔离（A 请求事件不进 B 断言集）+ scope 销毁后监听自动摘除（二次请求零重复）。#127 验收 3 只覆盖 Collector 服务撤除；#128 验收 1–4 没有隔离/摘除条款。这个回归锚不能丢——建议补进 **#128**（llm.Observe 的监听隔离与摘除是 Local 语义的直接验证点），#129 同理可选。

### 交叉审查发现（非阻塞，随实现落）

- **Source="bridge" 字面值语义漂移**：折叠主体从第三方桥变为各包自身后，`SourceBridge` 字符串值名不副实。重命名常量（值不变）或注释澄清（「运行期观测适配来源，历史名」），避免新读者误解。换出口的日志解析兼容性也一并说明。
- **重复 Observe 无幂等约束**：同 scope 两次 `llm.Observe` = 双监听双记录。验收补一条「重复调用幂等或 godoc 显式警告」。
- **ObserveConfig 校验**：Sink nil 时 AttachCollector / Observe 的行为未定义（#126 Attach 有 errNilSink 哨兵），子票实现对齐。
- **#129 枚举值精确** ✓：StopReason 实际值 completed/max_steps/canceled/error 与票面一致（loop.go:18–26）。
- genStart 并发覆盖问题从 #126 随迁（godoc 注明「同 scope 并发生成时计时不定义」）。

### 四票分工审查

分工边界清晰：各包折叠各包事件（#128 不订阅 loop.*、#129 不订阅 llm.*）、flow 走 Observer seam 不走事件（#130）、跨包口径（turn_end 不记 token）由 #129 折叠函数落实。验收矩阵无重叠无遗漏——除上述第 2 点的隔离锚归位外，覆盖完整。验收 5 把 gofmt 写进总验收（#126 的教训吸收）✓。

**结论**：补 D5 修订进迁移清单（#127）+ 隔离锚归位（#128）后，四票可开工；#129/#130 无阻塞。
